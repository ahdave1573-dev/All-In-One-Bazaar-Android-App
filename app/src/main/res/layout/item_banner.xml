<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="8dp"
    app:cardCornerRadius="24dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_banner_gradient">

        <!-- Background Decor -->
        <View
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="-50dp"
            android:layout_marginEnd="-50dp"
            android:background="@drawable/bg_circle_light"
            android:backgroundTint="@color/ocean_card"
            android:alpha="0.15" />

        <!-- Product Image (Right Side) -->
        <ImageView
            android:id="@+id/imgBannerProduct"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp"
            android:scaleType="centerCrop" />

        <!-- Content (Left Side) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_toStartOf="@id/imgBannerProduct"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:paddingStart="24dp">

            <TextView
                android:id="@+id/txtOfferTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="#330C3A8A"
                android:paddingHorizontal="12dp"
                android:paddingVertical="6dp"
                android:text="EXCLUSIVE DEAL"
                android:textColor="@color/ocean_text_inverse"
                android:textSize="10sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/txtBannerTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Product\nName"
                android:textColor="@color/ocean_light"
                android:textSize="22sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingMultiplier="0.9" />

            <androidx.cardview.widget.CardView
                android:id="@+id/btnShopNowBanner"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginTop="12dp"
                app:cardBackgroundColor="@color/ocean_card"
                app:cardCornerRadius="18dp"
                app:cardElevation="2dp">

                <TextView
                    android:id="@+id/txtBannerPrice"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:paddingHorizontal="20dp"
                    android:text="Shop Now →"
                    android:textColor="@color/ocean_primary"
                    android:textSize="13sp"
                    android:textStyle="bold" />
            </androidx.cardview.widget.CardView>
        </LinearLayout>
    </RelativeLayout>
</androidx.cardview.widget.CardView>
